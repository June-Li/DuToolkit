我会给你一个列表，列表中的元素是章节标题，请根据这些章节标题生成一个章节，我接下来会给你一个示例：
输入标题列表：
['一、章节标题', '1.1 章节标题', '二、章节标题', '2.1 章节标题', '2.1.1 章节标题', '三、章节标题']
输出章节：
[['1', '一、章节标题'], ['1.1', '1.1 章节标题'], ['2', '二、章节标题'], ['2.1', '2.1 章节标题'], ['2.1.1', '2.1.1 章节标题'], ['3', '三、章节标题']]

注意1: 以列表返回，列表长度和原始列表长度一致(长度一致要重点注意)，但是输出列表的元素变成了一个列表，这个子列表又包括了两个元素（重点注意），第一个元素为章节标题号，第二个元素为对应的原始标题字符串;
注意2：不要给我输出任何其他内容，我只要这个列表。
注意3：如果发现内容中有敏感词影响到大模型生成，请绕开这些敏感词继续为每个元素生成章节序号，也同样要注意输出列表长度不能变。
注意4：如果发现输入标题列表中有不是标题的元素，也要强制把他作为标题，否则的话列表长度就对不上了；
注意5：再次强调，输出列表长度和原始列表长度一致，不能多也不能少，否则的话列表长度就对不上了，请反复检查长度是否一致，不一致重新生成，直到长度一致为止再给我结果。
注意5：不要自己臆想，凭空出现新的标题，也不要把输入列表里的元素拆分成好几条，你就严格按照我给你的列表中的元素给每个元素赋予标题号即可，不要做其他多余的动作，不要凭空产生，也不要拆分元素；
注意6：你需要判断一下开头的一些元素是否是目录，如果是目录，那直接标注为None，因为目录内容会和之后的内容重复.

你需要帮我处理的列表是：
